在[并行计算](../Page/并行计算.md "wikilink")领域，**加速比**用于表示当[并行算法与对应的顺序执行](https://zh.wikipedia.org/wiki/并行算法 "wikilink")[算法](../Page/算法.md "wikilink")相比较时，速度快了多少。

## 定义

加速比以如下公式定义：

\[S_p = \frac{T_1}{T_p}\]

其中：

  - \(p\)指CPU数量
  - \(T_1\)指顺序执行[算法](../Page/算法.md "wikilink")的执行时间
  - \(T_p\)指当有p个[处理器时](https://zh.wikipedia.org/wiki/CPU "wikilink")，[并行算法的执行时间](https://zh.wikipedia.org/wiki/并行算法 "wikilink")

当\(\,S_p = p\)时，\(\,S_p\)便可称为“线性加速比”（，又名“理想加速比”）。当某一并行算法的加速比为理想加速比时，若将处理器数量加倍，执行速度也会加倍，即如“理想”之意，有“优秀的[可扩展性](https://zh.wikipedia.org/wiki/可扩展性 "wikilink")”。

### “效率”

由加速比衍生出的效率（）则是量度性能的指标，并如下定义：

\[E_p = \frac{S_p}{p} = \frac{T_1}{pT_p}\]. 效率\(E_p\)的值一般介于0～1之间，用于表示在解决问题时，相较于在通信与同步上的花费，参与计算的处理器得到了什么程度的充分利用。由定义易见，拥有线性加速比的算法与在单处理器上执行的算法的效率为1。

## 超线性加速比

在[并行计算](../Page/并行计算.md "wikilink")过程中，有时会遇到加速比\(\,S_p\)比处理器数\(p\)更大的情况，此时求得的加速比即称超线性加速比（）。超线性加速比很少出现，且由于初学者常以为理论上加速比的最大值应与参与计算的处理器的总数\(p\)相等，这一情况常使初学者迷惑不已。

超线性加速比有几种可能的成因，如现代计算机的[存储层次不同所带来的](https://zh.wikipedia.org/wiki/存储层次 "wikilink")“[高速缓存效应](https://zh.wikipedia.org/wiki/高速缓存 "wikilink")”；具体来说，较之顺序计算，在并行计算中，不仅参与计算的处理器数量更多，不同处理器的高速缓存也集合使用。而有鉴于此，集合的缓存便足以提供计算所需的存储量，算法执行时便不必使用速度较慢的内存，因而存储器读些时间便能大幅降低，这便对实际计算产生了额外的加速效果\[1\]。

## 参考资料

  -
[Category:并发计算](https://zh.wikipedia.org/wiki/Category:并发计算 "wikilink")

1.
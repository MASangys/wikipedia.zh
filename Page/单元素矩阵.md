在数学中，**单元素矩阵**是一个[矩阵](../Page/矩阵.md "wikilink")，其中只有一个元素为一，其余元素为零，\[1\]\[2\]例如：

\[\mathbf{J}^{23} = \left[\begin{matrix}
0 & 0 & 0 \\ 0 & 0 & 1 \\ 0 & 0 & 0 \end{matrix}\right].\]

它是[稀疏矩阵的特定类型](../Page/稀疏矩阵.md "wikilink")。
当单元素矩阵在矩阵的左侧相乘时，可以将其视为行选择器，例如：

  -
    <math>

`\mathbf{J}^{23}\mathbf{A} = \left[ \begin{matrix} 0 & 0& 0 \\ a_{31} & a_{32} & a_{33} \\ 0 & 0 & 0 \end{matrix}\right].`

</math>

或者，在右边乘以列选择器：

  -
    <math>

`\mathbf{A}\mathbf{J}^{23} = \left[ \begin{matrix} 0 & 0 & a_{12} \\ 0 & 0 & a_{22} \\ 0 & 0 & a_{32}  \end{matrix}\right].`

</math>

单元素矩阵这个名称并不常见，但在一些书籍中可以看到。<ref>例子：

  -
  - </ref>

## 参考文献

[Category:矩阵](https://zh.wikipedia.org/wiki/Category:矩阵 "wikilink")

1.
2.
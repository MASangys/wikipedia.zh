**多项式矩阵**，也称为**－矩阵**、**矩阵系数多项式**（不是**矩阵多项式**），是[数学](../Page/数学.md "wikilink")中[矩阵论里的概念](https://zh.wikipedia.org/wiki/矩阵论 "wikilink")，指系数是[多项式的](https://zh.wikipedia.org/wiki/多项式 "wikilink")[方块矩阵](../Page/方块矩阵.md "wikilink")。使用“－矩阵”的名称时，说明系数多项式以为不定元。

## 严格定义

给定[自然数](../Page/自然数.md "wikilink")和系数[环](https://zh.wikipedia.org/wiki/环 "wikilink")\(\mathbf{R}\)，一个阶多项式矩阵为如下形式：

\[A (\lambda ) = \left[ a_{i,j}(\lambda) \right]_{1 \leqslant i, j \leqslant n}, \quad \forall 1 \leqslant i, j \leqslant n, \; \; a_{i, j} (\lambda) = \sum_{k=0}^{d_{i, j}} a_{i, j, k} \lambda^k \in \mathbf{R}\left[ \lambda \right]\]， 其中\(d_{i, j}\)是每个多项式\(a_{i,j}(\lambda)\)的次数。如果设其中最大的为\(d\)：

\[d = \max_{1 \leqslant i, j \leqslant n} \{ d_{i, j} \}\] 那么多项式矩阵也可以表达为：

\[A = \sum_{k=0}^d \lambda^k \left[ a_{i,j, k} \right]_{1 \leqslant i, j \leqslant n} = \sum_{k=0}^d A(k) \lambda^k.\] 其中约定当\(k > d_{i, j}\)时，\(a_{i,j, k} = 0\).

由于多项式矩阵也能被表达为以（数值）矩阵为系数的多项式，所以也被称为矩阵系数多项式。如果最高次系数矩阵\(A(d)\)的[行列式](../Page/行列式.md "wikilink")不为零，则称多项式矩阵为为正则多项式矩阵（）。所有阶多项式矩阵的[集合记为](https://zh.wikipedia.org/wiki/集合 "wikilink")\(\mathcal{M}_{n}(\mathbf{R}\left[ \lambda \right])\)或\(\mathcal{M}_{n}(\mathbf{R})\left[ \lambda \right]\)。前者表示所有以多项式为系数的阶方块矩阵的集合，后者表示所有阶方块矩阵为系数的多项式的集合。可以验证两者是同构的。

## 例子

所有的数值矩阵都是多项式矩阵，因为可以将每个元素看成一个零多项式。设系数环为[实数](../Page/实数.md "wikilink")[域](https://zh.wikipedia.org/wiki/域 "wikilink")，以下是一个3阶多项式矩阵：

\[P=\begin{pmatrix}
1 & x^2 & x \\
0 & 2x & 2 \\
3x+2 & x^2-1 & 0
\end{pmatrix}
=\begin{pmatrix}
1 & 0 & 0 \\
0 & 0 & 2 \\
2 & -1 & 0
\end{pmatrix}

+\begin{pmatrix}
0 & 0 & 1 \\
0 & 2 & 0 \\
3 & 0 & 0
\end{pmatrix}x+\begin{pmatrix}
0 & 1 & 0 \\
0 & 0 & 0 \\
0 & 1 & 0
\end{pmatrix}x^2.\]

**[特征矩阵](https://zh.wikipedia.org/wiki/特征多项式 "wikilink")**是多项式矩阵的一个例子。设有阶数值矩阵，则特征矩阵实际上是一次多项式矩阵：\(P_A (\lambda) = \lambda \mathbf{I}_n - A\)。而特征矩阵的行列式\(\det \left( \lambda \mathbf{I}_n - A \right)\)就是数值矩阵的[特征多项式](https://zh.wikipedia.org/wiki/特征多项式 "wikilink")。

## 性质

由于多项式代数和矩阵代数的结构特性，环\(\mathbf{R}\)上的所有阶多项式矩阵也构成一个[代数](../Page/代數_\(環論\).md "wikilink")。两个阶多项式矩阵可以互相加减、相乘，并且满足加法交换律和乘法分配律（不满足乘法交换律）。用与数值矩阵相同的方式可以定义多项式矩阵的[初等变换](https://zh.wikipedia.org/wiki/初等变换 "wikilink")、[相似关系](https://zh.wikipedia.org/wiki/相似矩阵 "wikilink")、[等价关系](../Page/等价关系.md "wikilink")（也称为**相抵**）、[秩以及](https://zh.wikipedia.org/wiki/矩阵的秩 "wikilink")[行列式](../Page/行列式.md "wikilink")。

如果系数环是域，那么可以证明，所有的多项式矩阵都可以[对角化](https://zh.wikipedia.org/wiki/矩阵对角化 "wikilink")。任何一个[秩为](https://zh.wikipedia.org/wiki/矩阵的秩 "wikilink")的多项式矩阵，都可以相抵于一个对角多项式矩阵：

\[a\operatorname{diag} (d_1(\lambda), d_2(\lambda), \cdots , d_r(\lambda), 0, \cdots , 0)\] 其中的每个非零的对角元素\(d_i(\lambda)\)都是[首一多项式](https://zh.wikipedia.org/wiki/首一多项式 "wikilink")，并且[整除下一个对角元素](https://zh.wikipedia.org/wiki/整除 "wikilink")\(d_{i+1}(\lambda)\)。这种形式称为多项式矩阵的史密斯标准型（），所有的\(d_i(\lambda)\)被称为原多项式矩阵的[不变因子](../Page/不变因子.md "wikilink")。

如果将阶多项式矩阵看成以阶方块矩阵为系数的多项式，可以通过将其中的不定元替换为一个阶方块数值矩阵，而得到一个阶数值矩阵。这种操作称为多项式矩阵的矩阵替换。由于矩阵乘法不满足交换律，所以替换分为左替换和右替换：

  -
    左替换：将\(\sum_{k=0}^d A(k) \lambda^k\) 替换为 \(\sum_{k=0}^d B^k \cdot A(k),\) 也记作\(P_l^A (B),\)
    右替换：将\(\sum_{k=0}^d A(k) \lambda^k\) 替换为 \(\sum_{k=0}^d  A(k)\cdot B^k,\) 也记作\(P_r^A (B).\)

如果系数环是域，那么多项式矩阵之间可以做[带余除法](../Page/带余除法.md "wikilink")：如果\(A(\lambda)\)和\(B(\lambda)\)都是多项式矩阵，其中\(B(\lambda) \neq 0\)，那么唯一存在多项式矩阵\(Q(\lambda)\)和\(R(\lambda)\)，满则

1.  \(A(\lambda) = B(\lambda) Q(\lambda) + R (\lambda),\)
2.  \(R(\lambda)\)作为多项式的次数严格小于\(B(\lambda)\)，或者为零。

## 参见

  - [特征多项式](https://zh.wikipedia.org/wiki/特征多项式 "wikilink")
  - [不变因子](../Page/不变因子.md "wikilink")
  - [初等因子](https://zh.wikipedia.org/wiki/初等因子 "wikilink")

## 参考来源

[Category:矩阵论](https://zh.wikipedia.org/wiki/Category:矩阵论 "wikilink") [Category:多项式](https://zh.wikipedia.org/wiki/Category:多项式 "wikilink")